version: "3.9"

services:
  kds-backend:
    build: .
    container_name: kds-backend
    network_mode: "host" 
    ports:
      - "3001:3001"
    env_file:
      - ./.env
    restart: unless-stopped
